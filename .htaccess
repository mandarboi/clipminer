<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /clipminer/

    # Jangan rewrite file & folder yang ada
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d

    # Semua request ke index.php
    RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule>
